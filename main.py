import numpy as np
import matplotlib.pyplot as plt
import pandas as pd

'''data = pd.read_csv('realEstateAbridged.csv',sep=',')
col = list(data.columns)

X = np.array(pd.DataFrame(data, columns=[col[3]]))
Y = np.array(pd.DataFrame(data, columns=[col[7]]))

X = np.insert(X, 0, 1, axis=1)'''